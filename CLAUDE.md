# Claude Code — A Day (batagor)

The full rules for working in this repo live in [`AGENTS.md`](AGENTS.md) — read
that file, not this one. This pointer exists only because Claude Code looks for
`CLAUDE.md` specifically; keep all actual rules content in `AGENTS.md` so other
AI tools (which read `AGENTS.md`) stay in sync automatically.
